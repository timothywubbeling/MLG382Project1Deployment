[variables]
PYTHON_VERSION = "3.11"

[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = [
  "python -m venv .venv",
  ". .venv/bin/activate",
  "pip install --upgrade pip",
  "pip install -r requirements.txt"
]
